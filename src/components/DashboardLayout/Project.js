import React from "react";

const Project = ({ title }) => {
  return (
    <div className="dashboardLayout_section_projects_link_project">
      <span>{title}</span>
    </div>
  );
};

export default Project;
